# Test-only dependency tree. The shipped app has no dependencies at all -
# nothing in here is served, and GitHub Pages never sees it.
node_modules/

# macOS Finder metadata
.DS_Store

# Local-only project reference. Never published to the public repo.
PROGRESS.md
CLAUDE.md

# Local dev only. The shipped app is static files served straight from the
# repo root - nothing here is part of it, and GitHub Pages must not serve it.
server.mjs
.claude/
