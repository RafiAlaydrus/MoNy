{
  "name": "monthly-money-tracker",
  "version": "1.13.0",
  "private": true,
  "description": "Offline-first monthly money tracker (vanilla PWA). No build step - package.json exists only to run the tests.",
  "scripts": {
    "test": "node --test test/"
  }
}
